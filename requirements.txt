# CPU-only torch wheels -- sentence-transformers pulls torch transitively, and
# the default PyPI wheel bundles CUDA binaries (~2GB) that a CPU-only host like
# Render doesn't need and can't fit in a free-tier build. This index carries
# CPU-only builds; it's consulted for ALL packages here, including transitive
# ones, not just ones pinned below.
--extra-index-url https://download.pytorch.org/whl/cpu

anthropic==0.40.0
sentence-transformers==3.3.1
numpy
python-dotenv==1.0.1
fastapi==0.115.6
uvicorn[standard]==0.32.1
pytest==8.3.4
httpx==0.27.2
