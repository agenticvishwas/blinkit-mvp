anthropic==0.40.0
# Keyword (BM25) retrieval, not embeddings -- see agent/retrieval.py's revision
# note and the root README's "Retrieval strategy" section. rank-bm25 depends
# only on numpy; this replaces sentence-transformers + torch (~2GB, ~90s cold
# load), which was too heavy for a free-tier host's memory.
rank-bm25==0.2.2
numpy
python-dotenv==1.0.1
fastapi==0.115.6
uvicorn[standard]==0.32.1
pytest==8.3.4
httpx==0.27.2
